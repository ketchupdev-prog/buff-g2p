{
  "name": "buffr-g2p",
  "version": "1.0.0",
  "private": true,
  "description": "Buffr G2P – backend (Node), mobile (Expo), Buffr AI (Python).",
  "scripts": {
    "db:check": "npm run db:check --prefix backend",
    "migrate": "npm run migrate --prefix backend",
    "test": "npm run test --prefix backend && npm run test --prefix mobile",
    "test:backend": "npm run test --prefix backend",
    "test:mobile": "npm run test --prefix mobile",
    "test:ai": "cd backend && PYTHONPATH=. python scripts/verify_buffr_ai.py",
    "test:ai-unit": "npm run test:ai-unit --prefix backend"
  }
}
